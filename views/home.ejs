<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Anonymous Chat Landing</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    @keyframes marquee {
      0% { transform: translateX(0); }
      100% { transform: translateX(-50%); }
    }

    .animate-marquee {
      display: flex;
      width: fit-content;
      animation: marquee 20s linear infinite;
    }
  </style>
</head>
<body class="bg-zinc-900 overflow-x-hidden flex flex-col min-h-screen">

  <!-- Header -->
  <div class="w-full h-12 flex items-center bg-zinc-500 px-4 sm:px-6 md:px-20">
    <h1 class="tracking-tighter text-xl sm:text-2xl md:text-3xl text-black underline decoration-2">Anonymus.</h1>
    <button onclick="window.location.href='/login'" class="mb-2 ml-auto mt-1 sm:mt-2 mr-2 sm:mr-4 px-3 py-1 bg-blue-500 rounded-md text-white text-sm sm:text-lg hover:bg-blue-800 transition">Login</button>
    <button onclick="window.location.href='/create'" class="mb-2 mt-1 sm:mt-2 mr-2 sm:mr-4 px-3 py-1 bg-white rounded-md text-blue-600 text-sm sm:text-lg hover:bg-gray-300 transition">Create</button>
     <button onclick="window.location.href='/feed'" class="mb-2 mt-1 sm:mt-2 mr-2 sm:mr-4 px-3 py-1 bg-green-500 rounded-md text-white text-sm sm:text-lg mb-1 hover:bg-green-800 transition font-bold tracking-tighter">Feed</button>
  </div>

  <!-- Top Marquee (closer to header) -->
  <div class="w-full bg-indigo-900/50 border-y border-indigo-700 py-2 sm:py-4 mt-2 sm:mt-20 overflow-hidden whitespace-nowrap relative">
    <div class="animate-marquee">
      <span class="text-indigo-200 text-sm sm:text-lg font-semibold mx-2 sm:mx-4">
        Anonymous messages are flowing! &rarr; Join the conversation &rarr; Share your ideas freely &rarr; Read, react, chat anonymously &rarr;
      </span>
      <span class="text-indigo-200 text-sm sm:text-lg font-semibold mx-2 sm:mx-4">
        Anonymous messages are flowing! &rarr; Join the conversation &rarr; Share your ideas freely &rarr; Read, react, chat anonymously &rarr;
      </span>
    </div>
  </div>

  <!-- Main Headlines -->
  <div class="flex flex-col items-center mt-6 sm:mt-12 md:mt-16 mb-8 sm:mb-12">
    <h1 class="text-orange-200 text-center text-xl sm:text-2xl md:text-3xl underline decoration-orange-800 rounded-md bg-orange-400/60 max-w-xs sm:max-w-md md:max-w-lg px-2 sm:px-4 py-2">
      Your place to post whatever you want.😉 
    </h1>

    <h1 class="text-green-200 text-center mt-4 sm:mt-6 text-lg sm:text-2xl md:text-2xl underline decoration-green-800 rounded-md bg-green-400/60 max-w-xs sm:max-w-md md:max-w-lg px-2 sm:px-4 py-2">
      Effortless Posting.🤠 
    </h1>
  </div>

  <!-- Bottom Marquee (closer to footer) -->
  <div class="w-full bg-indigo-900/50 border-y border-indigo-700 py-2 sm:py-4 mb-16 overflow-hidden whitespace-nowrap relative">
  <div class="animate-marquee">
    <span class="text-indigo-200 text-sm sm:text-lg font-semibold mx-2 sm:mx-4">
      Anonymous messages are flowing! &rarr; Join the conversation &rarr; Share your ideas freely &rarr; Read, react, chat anonymously &rarr;
    </span>
    <span class="text-indigo-200 text-sm sm:text-lg font-semibold mx-2 sm:mx-4">
      Anonymous messages are flowing! &rarr; Join the conversation &rarr; Share your ideas freely &rarr; Read, react, chat anonymously &rarr;
    </span>
  </div>
</div>

  <!-- Fixed Footer -->
  <div class="fixed bottom-0 left-0 w-full bg-gray-500 p-2 sm:p-4 text-white text-center flex flex-col sm:flex-row justify-center sm:justify-between items-center">
    <span class="font-bold bg-gradient-to-r from-black via-zinc-800 to-neutral-700 bg-clip-text text-transparent underline decoratin-2 decoration-black">Hope you have a great time here! || Developed by Vivek Rakesh Tiwary.</span>
    <span id="datetime" class="mt-1 sm:mt-0 text-black font-semibold"></span>
  </div>

  <script>
    function updateDateTime() {
      const now = new Date();
      const options = {
        weekday: 'short', year: 'numeric', month: 'short',
        day: 'numeric', hour: '2-digit', minute: '2-digit', second: '2-digit'
      };
      document.getElementById('datetime').textContent = now.toLocaleString('en-US', options);
    }
    updateDateTime();
    setInterval(updateDateTime, 1000);
  </script>

</body>
</html>
